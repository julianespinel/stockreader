api:
  build: .
  command: python3 src/stockreader.py config-docker.toml
  ports:
    - "5000:5000"
  links:
    - db
db:
  image: mongo:3
  ports:
    - "27017:27017"
  volumes:
    - /data/db/:/data/db
